export type CameraFacing = 'front' | 'back';

export type PreviewRect = {
  x: number;
  y: number;
  width: number;
  height: number;
};

export type PreviewSize = {
  width: number;
  height: number;
};
